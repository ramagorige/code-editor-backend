Real-Time Code Editor Backend (Spring Boot + Gemini AI)

This is a Java Spring Boot backend application for a real-time collaborative code editor.  
It uses Google Gemini API to provide AI-powered code completion suggestions based on the input

Features

-  AI code completion using Gemini API
-  RESTful API built with Spring Boot
-  Clean modular structure (Controller, Service, Model)
-  Configurable API key via `application.yml`
-  JSON-based request and response for easy frontend integration
-  Supports multiple programming languages (JS, TS, Java, etc.)

## Tech Stack:
 Language | Java 17+ 
 Framework | Spring Boot 3.x 
 Build Tool | Maven |
 HTTP Client | Java 11+ `HttpClient
 JSON Parser | Jackson (`ObjectMapper`)
  AI API | Google Gemini (Generative Language API) 
  IDE | Eclipse

-> Set Up, Build, and Run the Backend (Java Spring Boot)

Before running the backend, ensure these tools are installed:

- Java 17 or above

Check version: java -version in command promt

 Maven 3.8+

Check version:  mvn -version (cmd)

 IDE (Eclipse/IntelliJ IDEA)

2. Download or Clone the Backend Project:

Option A: Clone from GitHub
git clone https://github.com/<your-username>/<your-repo>.git
cd <your-repo>

Option B: Import ZIP into IDE

-> Download the ZIP

 -> Extract it

Open Eclipse → File → Import → Maven → Existing Maven Projects

 3. Configure the Application (Important)

Find the file:

src/main/resources/application.yml


Add your Gemini API key:

server:
  port: 8080

gemini:
  api:
    key: "YOUR_GEMINI_API_KEY"


 Replace "YOUR_GEMINI_API_KEY" with your actual key.

4. Build the Backend Using Maven

Open terminal in your backend project folder and run:

mvn clean install

-> This does three things:  Cleans old build files, Downloads dependencies, Compiles all Java files

 5. Run the Backend Application
Option A: Run from command line
mvn spring-boot:run

Option B: Run in Eclipse

Right-click CodeEditorApplication

Select Run As → Java Application

After building, check: target/editor-0.0.1-SNAPSHOT.jar

6. Test the API in (Postman)

Make a POST request:  ->   (POST)   http://localhost:8080/api/complete

Request Body:
{
  "code": "function hello(name){ console.log(name); }",
  "cursor": 25
}

Expected Response:
{
  "suggestions": [
    {
      "title": "AI Suggestion",
      "content": "some code completion from Gemini",
      "source": "Generated by Gemini API"
    }
  ]
}

High-Level Architecture Diagram
              
      (1) REST API: Send code + cursor
                           │
               ┌───────────▼──────────────┐
               │   Backend (Spring Boot)   │
               │ ─ REST Controller         │
               │ ─ GeminiService           │
               │ ─ WebSocket for syncing   │
               └───────────┬──────────────┘
                           │
      (2) HTTP request to Gemini AI API
                           │
               ┌───────────▼──────────────┐
               │       Gemini API          │
               │  Processes prompt + code  │
               │  Returns suggestion text  │
               └───────┬──────────────────┘
                       │
       (3) Send AI suggestions back
                       │
              ┌────────▼──────────┐
              │  Frontend Editor  │
              │ Shows completion  │
              └────────────────────┘
2️.Backend (Java Spring Boot): 

The backend acts as a bridge between the frontend and Google Gemini API.

Responsibilities:

- Expose REST API endpoint    --/api/complete
- Accept code + cursor position
 - Format the prompt for Gemini
- Call Gemini API
- Parse Gemini’s response
- Return a clean suggestion to frontend
- Provide WebSocket communication for real-time collaboration

Backend Functional Flow:  Frontend → POST /api/complete → Backend → Gemini API → Backend → Frontend

Key backend classes:
CompletionController → Receives request from frontend
CompletionRequest → Holds code + cursor
GeminiService → Calls Gemini API
CompletionResponse → Sends processed suggestion back

The frontend sends:

{
  "code": "function test() { }",
  "cursor": 16
}

Step 2 — Backend receives    --/api/complete

Backend forwards a formatted AI request to Gemini API.

Step 3 — Gemini generates suggestion

Example response:

"Consider adding a parameter to improve flexibility..."

Step 4 — Backend maps response

Backend sends this to frontend as:

{
  "suggestions": [
    {
      "title": "AI Suggestion",
      "content": "Consider adding parameters to improve function flexibility.",
      "source": "Generated by Gemini API"
    }
  ]
}

then successfully run this application.